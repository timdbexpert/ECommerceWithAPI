@model IEnumerable<ECommerce_MVC.Areas.Admin.ViewModel.SubMenuViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center">Submenus</h2>

<p>
    <a asp-area="Admin" asp-controller="SubMenu" asp-action="Create">Create New SubMenu</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MenuId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MenuId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit","SubMenu", new {  id=item.Id  }) |
                @Html.ActionLink("Details", "Details","SubMenu", new {  id=item.Id }) |
                @Html.ActionLink("Delete", "Delete","SubMenu", new {  id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
